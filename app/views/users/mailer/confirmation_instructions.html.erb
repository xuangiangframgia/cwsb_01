<p><%= "#{t("users.mailer.confirm.welcome")} @email" %>!</p>

<p><%= t "users.mailer.confirm.remind" %></p>

<p><%= link_to t("users.mailer.confirm.confirm_account"),
  confirmation_url(@resource, confirmation_token: @token) %></p>
